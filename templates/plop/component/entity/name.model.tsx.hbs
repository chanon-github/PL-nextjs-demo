/**
 *  {{> license }}
 *  {{pascalCase name}} - Model
 */

export namespace {{pascalCase name}}Props {
  export interface {{#if (isEquals type "FULL")}}Container{{else}}Main{{/if}} {
    //
  }
  {{#if (isEquals type "FULL")}}

  export interface Content {
    //
  }
  {{/if}}
}